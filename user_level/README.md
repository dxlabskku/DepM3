# DepM³ — User-level Training (MUD3)

Entry point: `main_mud3.py` (see `--help`). Expects `$MUD3_DATA_ROOT`
(default `$DEPM3_DATA_ROOT/MUD3`) containing `labels.csv`, `dep_feat.pkl`,
`nondep_feat.pkl`.
